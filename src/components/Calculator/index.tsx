import { Container } from "./styles"

export const Calculator: React.FC = () => {
  return (
    <Container>
      {/* display */}
      {/* 
            buttons 
        numbers || actions
      */}
    </Container>
  )
}