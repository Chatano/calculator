import { Container } from "../styles/components/Calculator"

export const Calculator: React.FC = () => {
  return (
    <Container>
      {/* display */}
      {/* 
            buttons 
        numbers || actions
      */}
    </Container>
  )
}